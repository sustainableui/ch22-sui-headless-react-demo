import { Button, ButtonGroup } from '@mui/material';

function Switch() {
  return (
    <ButtonGroup variant="contained">
      <Button>One</Button>
      <Button>Two</Button>
      <Button>Three</Button>
    </ButtonGroup>
  );
}

export default Switch;
